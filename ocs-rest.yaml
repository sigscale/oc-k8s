kind: Service
apiVersion: v1
metadata:
  name: ocs-rest
  labels:
    app.kubernetes.io/name: ocs
spec:
  selector:
    app.kubernetes.io/component: 3gpp-ocf
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
